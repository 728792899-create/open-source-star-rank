---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="数据口径｜开源星榜"
  description="了解昨日 Star 净增榜的候选池范围、零点快照、连续窗口和公开数据边界。"
>
  <main id="main-content" class="methodology shell">
    <p class="eyebrow">Methodology · observed net growth</p>
    <h1>昨日净增，<br /><em>每个窗口都说清。</em></h1>
    <p class="methodology-lead">开源星榜以“昨日净增榜”为每日主榜，并提供 7 日、30 日、语言和全部历史星标榜。日榜来自持续追踪的候选池，不宣称覆盖 GitHub 全站。</p>

    <section>
      <span class="section-number">01</span>
      <div>
        <h2>昨日净增榜如何计算</h2>
        <div class="formula"><span>昨日净增</span><strong>=</strong><span>本次 Star 总数</span><strong>−</strong><span>上次 Star 总数</span></div>
        <p>采集任务在北京时间每日 00:20 调度，通过相邻两次零点快照计算上一完整自然日的 Star 总数变化。净增包含用户取消 Star 的影响，因此数字可以为负。</p>
        <p>有效快照必须位于北京时间 00:00–03:00，相邻日期连续且实际间隔为 21–27 小时。新发现项目必须拥有两个有效快照后才能参与比较。</p>
        <p>缺失、无效日期不补采、不补零、不插值。排序依次为净增 Star、当前总 Star、仓库全名。</p>
      </div>
    </section>

    <section>
      <span class="section-number">02</span>
      <div>
        <h2>候选池覆盖范围</h2>
        <p>每天通过 GitHub Repository Search 合并最近创建、近期活跃和成熟活跃项目，最多保留 2,000 个非 Fork、未归档、未禁用的公开仓库。</p>
        <p>GitHub Search API 每条搜索最多返回 1,000 个结果，也不能按每日 Star 增量排序，因此昨日净增榜是候选池观测榜，不是 GitHub 全站绝对完整排行。</p>
        <p>语言榜在当日全部可比较候选项目中独立排名，最多发布 500 项；7 日和 30 日榜只使用完整的连续有效窗口。</p>
        <a class="text-link" href="https://docs.github.com/en/rest/search/search" target="_blank" rel="noreferrer">查看 GitHub Search API 限制 →</a>
      </div>
    </section>

    <section>
      <span class="section-number">03</span>
      <div>
        <h2>失败时如何保护数据</h2>
        <p>API 限流、响应不完整、快照无效、JSON Schema 不兼容或站点构建失败时，任务不会提交半成品，也不会替换上一版网站。</p>
        <p>同一自然日普通重跑复用已有有效快照，不改变统计窗口。历史日榜永久保留，完整候选快照按保留策略维护。</p>
        <p>项目按 repository ID 跟踪，仓库重命名不会产生新项目；删除、转私有、Fork、归档或禁用仓库不会继续进入新榜。</p>
      </div>
    </section>

    <section>
      <span class="section-number">04</span>
      <div>
        <h2>中文项目名称与简介</h2>
        <p>中文功能名和简介用于帮助中文用户快速理解项目，由 GitHub Models 根据公开仓库名称和原始描述生成，并经过结构、长度和关键标识校验。</p>
        <p>页面始终保留 GitHub 的 owner/repository 原名和跳转链接。用户可以切换“中文 / 原文”；译文不可用时直接显示 GitHub 原文，不阻塞榜单发布。</p>
      </div>
    </section>

    <section>
      <span class="section-number">05</span>
      <div>
        <h2>项目方向、产品形态与适用场景</h2>
        <p>分类是辅助理解层。模型从版本化固定词表中选择一个主方向、一个产品形态和 1–4 个适用场景，人工修正优先于模型结果。</p>
        <p>语言、方向、形态和场景可组合筛选。筛选结果从榜单深度池中重新排列，最多显示 500 项，并同时保留原总榜名次。</p>
      </div>
    </section>

    <section>
      <span class="section-number">06</span>
      <div>
        <h2>全部历史星标 Top 1000</h2>
        <p>历史星标榜按 GitHub 搜索的累计 Star 数降序收录前 1,000 个有效公开仓库，并通过 Star 区间分段回填被过滤项目留下的空位。</p>
        <p>该榜每周更新一次，反映长期累计关注度，不统计每日变化，也不扣除取消 Star。</p>
      </div>
    </section>

    <section>
      <span class="section-number">07</span>
      <div>
        <h2>项目日期、倒计时与可选 GitHub 登录</h2>
        <p>“GitHub 创建”来自仓库 created_at，“最近代码推送”来自 pushed_at，“已发布”是从仓库创建时间到当前日期的自然日差。</p>
        <p>每个榜单根据对应索引中的调度时间显示更新倒计时。倒计时归零后会显示“等待自动发布”，直到公开索引实际更新。</p>
        <p>不登录即可使用本机收藏与对比。选择 GitHub 登录后，收藏会同步为用户账号上的真实 Star / Unstar；站点只申请 Metadata 读取与 Starring 读写权限。</p>
      </div>
    </section>
  </main>
</BaseLayout>
