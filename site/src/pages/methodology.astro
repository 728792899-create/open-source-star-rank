---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="数据口径｜开源星榜"
  description="了解开源星榜如何发现候选项目、计算每日 Star 净增，以及榜单的覆盖边界。"
>
  <main id="main-content" class="methodology shell">
    <p class="eyebrow">Methodology · v1.2</p>
    <h1>真实快照，<br /><em>透明边界。</em></h1>
    <p class="methodology-lead">开源星榜不是 GitHub 官方榜单，也不猜测热度。它每天观察一组公开仓库，用连续快照回答一个简单问题：过去一个统计窗口里，哪些项目获得了最多净新增 Star？</p>

    <section>
      <span class="section-number">01</span>
      <div>
        <h2>候选项目从哪里来</h2>
        <p>每天通过 GitHub Repository Search 合并三组项目：最近 30 天创建的高 Star 项目、最近 7 天活跃且至少 100 Star 的项目，以及至少 10,000 Star 的成熟活跃项目。配置中的公开仓库种子会固定保留。</p>
        <p>候选池最多保留 2,000 个非 Fork、未归档、未禁用的公开仓库。超限时优先保留近期确有增长、新发现或总 Star 更高的项目。</p>
      </div>
    </section>

    <section>
      <span class="section-number">02</span>
      <div>
        <h2>日净增如何计算</h2>
        <div class="formula"><span>当日净增</span><strong>=</strong><span>本次 Star 总数</span><strong>−</strong><span>上次 Star 总数</span></div>
        <p>采集任务在北京时间每日 00:20 调度，并保存实际执行时间。有效快照必须位于 00:00–03:00，相邻快照日期连续且实际间隔为 21–27 小时。只有同时满足这些条件的快照对才可生成榜单。</p>
        <p>新发现项目必须拥有两个有效快照才可比较。缺失、无效日期不补采、不补零、不插值；7 日和 30 日榜需要完整的连续窗口，不会用部分日期凑数。</p>
        <p>“净增”已经包含取消 Star 的影响，所以数字可能为负数。出现同分时依次比较当前总 Star，并以仓库全名稳定排序。</p>
      </div>
    </section>

    <section>
      <span class="section-number">03</span>
      <div>
        <h2>为什么不是全站完整榜</h2>
        <p>GitHub Search API 每条搜索最多返回 1,000 个结果，也不能按每日 Star 增量排序。零成本方案无法先知道全站哪个仓库增长最快，所以本站明确称为“候选池观测榜”。</p>
        <p>一个未进入候选池的旧项目即使突然爆红，也可能被漏掉；榜单适合发现信号，不适合作为 GitHub 全站统计证明。</p>
        <a class="text-link" href="https://docs.github.com/en/rest/search/search" target="_blank" rel="noreferrer">查看 GitHub Search API 限制 →</a>
      </div>
    </section>

    <section>
      <span class="section-number">04</span>
      <div>
        <h2>语言榜与项目历史</h2>
        <p>独立语言榜在当日全部可比较候选项目中排名，不是从全局 Top 100 中二次筛选。某语言至少有 5 个可比较项目才会发布，最多展示 50 项。</p>
        <p>项目页以 GitHub repository ID 作为稳定地址，因此仓库重命名不会丢失历史。近 30 日序列中的空值会原样显示为缺失，不被解释为零增长。</p>
      </div>
    </section>

    <section>
      <span class="section-number">05</span>
      <div>
        <h2>失败时如何保护数据</h2>
        <p>搜索结果不完整、API 限流、字段缺失、快照完整率不足 100% 或 JSON Schema 校验失败时，当日任务不会发布。历史数据保存在独立数据分支，网站继续展示上一份成功榜单。</p>
        <p>仓库以 GitHub repository ID 跟踪，因此重命名不会被当成新项目；删除、归档、禁用和转为 Fork 的项目会退出候选池。</p>
        <p>公开日榜永久保留，完整候选快照保留最近 90 个北京时间自然日。每次采集同时公开搜索覆盖、候选进出、API 请求和重试次数；数据超过 36 小时未更新时，页面会显示过期提示。</p>
      </div>
    </section>
  </main>
</BaseLayout>
