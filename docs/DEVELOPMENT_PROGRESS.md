# 开发进度与同步记录

## 2026-09-21 无人值守第二阶段：Cloudflare 运行器与验证备份

- 新增独立 ops-worker（Workers + R2）：15分钟检查固定提交和线上索引/业务摘要；有效窗口内补调度、已有原始记录离线恢复、固定SHA重部署。每日限次/冷却/运行中任务检查与R2条件锁限制重复执行；备份补调度独立失败隔离。默认自动恢复和Issue投递关闭。
- R2按内容摘要保存精确Git提交的JSON，读回校验后执行隔离恢复及完整业务校验，再提升最新备份指针。语义损坏、路径穿越、校验失败或已有恢复目录都会拒绝；历史清单保留。新增独立备份工作流，每次备份都有完整恢复验证。
- 采集按实际缺失刷新数预检API预算，Retry-After/reset累计等待不超过90秒，生产请求受03:00截止保护。可选GitHub App在Worker与采集工作流自动获取短期令牌；只读采集凭据不用于故障Issue。
- 翻译/分类持久队列按尝试时间轮换、指数退避并保留原业务优先级；上游不可用停止后续批次。源变化和人工完成清理旧等待，public-only不写状态。已校验补全与队列提前到前端构建前提交。新增有来源时间的轻量operations摘要，监测补全发布落后，不仅比较日榜索引。
- 验证：Python161项、Worker10项通过；工作流YAML解析、Worker配置与打包检查通过，依赖审计零告警。本机workerd/R2完成27份JSON整批上传/恢复，重复写入、摘要和401门禁通过。独立复核已修复备份指针、队列提交、告警令牌和public-only边界问题；远端CI以本提交Checks为准。上一阶段a31347d两项CI均已成功。
- 未部署：本机Cloudflare授权过期，未创建真实R2桶、配置GitHub App/共享密钥、启用Cron恢复或改变生产数据。真实云验收与31日连续观察尚未开始；按ops-worker/README.md启用，源码同步不等于无人值守已经运行。

## 2026-09-21 无人值守第一阶段：先保存采样，再发布

- 每批校验后的原始采样以 SHA-256 命名、不可变保存到 captures/；独立 latest 指针明确同时间戳替换的版本。派生榜单前写入恢复日志，故障后从原始记录按日期恢复，不重新请求 GitHub、不补造时间。
- 工作流在派生失败时仍单独提交已校验采样；完整数据校验与提交提前到字体安装、前端构建之前，网站构建故障不再丢掉采样。补推检查远端祖先与未发布提交，拒绝混入无关数据。
- 验证：Python 全量146项通过，其中7项新增覆盖派生失败、新目录离线恢复、多日恢复、损坏摘要、同时间替换、仅保存采样及推送失败重试。独立复核无待处理发现；未模拟真实断电或 GitHub 网络故障。
- 本阶段同步开发分支和 PR，不合并、不触发生产采集。Cloudflare 独立监测与备份继续实施；当前 Cloudflare 登录已过期，尚未启用。

## 2026-09-21 历史榜说明收纳

- 历史7/30日榜的整行黄色提示改为统计日期旁的“历史榜 · 查看说明”原生折叠入口；点击/键盘展开完整原因、真实日期和连续窗口进度，并可访问采样状态。正文以浮层呈现，不推低列表；再次操作可收起，无JS也可用。历史日期与原有新鲜度提示保持可见，不改变数据状态。
- 验证：320/390/768/1440宽度局部切换后可展开/收起，浮层未越界、无横向溢出，展开前后列表Y位移均为0；390/1440 Axe零违规、无脚本错误，无JS可展开，已检查手机与桌面截图。Astro89文件零诊断；真实5947页重建及静态产物校验通过，复用未变化分享图。独立只读复核无待处理发现。
- 本步仅开发分支与PR同步，本地预览已更新；远端CI以本提交Checks为准，未合并或部署。

## 2026-09-21 榜单初始化与分享定位收尾

- 首次无数据的昨日榜复用紧凑标题、信息轮播、四周期导航和小型进度信息；保留无效采样原因、计划时间及真实窗口规则。累计榜区分“尚未生成”与“筛选无匹配”，不再同时显示矛盾提示。
- 纯hash跳转保持原生定位，不重新请求整页；跟踪分页、筛选及中英文切换后的URL。跨周期后退及直接打开/刷新筛选分享链接，等待异步结果渲染后定位目标，并以导航版本、请求取消和首次URL校验阻止过时定位。
- 验证：31项站点单测、Astro89文件零诊断；真实5947页重建及产物校验通过（分享图未变，复用已验证产物）。独立seed空数据环境66页构建及新增初始化契约校验通过；390/1440浏览器四周期切换、无横向溢出、Axe零违规及无JS导航通过，未更换用户预览数据。导航与页头20项浏览器回归通过，新增3项覆盖hash、空数据和异步分享目标；定位断言校验整个项目行在视口内，兼容字体加载引起的正常布局位移。独立复核无剩余发现。
- 已核对上一轮c816571完整CI及Worker检查成功。当前步骤仅提交推送开发分支、更新PR；本提交远端Checks另行跟踪，未合并或部署。

## 2026-09-21 统一榜单布局与周期内切换

- 昨日、7日、30日和累计榜统一紧凑标题、更新信息条、周期导航与项目行；30日积累状态保留四个切换入口和清晰进度，不再使用大宣传式标题。累计榜补齐头像、项目详情及分享操作，按100项分页，保留四类筛选、原始排名、紧凑模式与JSON下载；没有累计趋势快照时明确显示暂无快照，不把日净增伪装为累计趋势。
- 四类榜单支持同文档局部切换，保留搜索/分类条件、内容语言和侧栏/密度偏好，同时同步URL、标题和SEO元数据，支持前进/后退；网络失败回退普通链接导航，无JS保留静态内容。归档事件/分类/语言页沿用普通导航，避免改变其独立脚本契约。
- 提取显式初始化函数；切换开始取消旧请求，销毁计时器、全局监听及图表观察器，重新挂载搜索框与榜单控件。独立复核指出的后退期间旧请求回写URL、快速点回当前周期及侧栏高亮问题均已修正。
- 验证：31项站点单测、Astro88文件零诊断；真实数据5947页/406张分享图构建与产物校验通过，打包后17项浏览器回归通过，含10项新增导航/累计榜回归。开发服务在分享卡渲染占用CPU期间发生的单项5秒等待超时，已在静态产物复测通过。累计榜及30日空状态在手机/桌面Axe均零违规、无脚本错误；空状态定义列表结构已修正并复测通过。最新远端完整CI以本提交Checks为准，未合并或部署。

## 2026-09-21 侧栏宽度与轮播文字对齐微调

- 桌面侧栏统一收窄至184px（原大屏236px、中屏208px），同步收紧菜单图标间距及内边距；移动抽屉由236px收窄至208px，保留原触控高度和收起偏好行为。
- 轮播项使用Flex垂直居中，修复共享最大行高时短文本贴顶的问题，仍为长内容保留稳定高度，避免切换跳动。
- 验证：既有首页7个浏览器场景全部通过；320/390/1024/1440/1920五种宽度逐一检查三条轮播，内容垂直中心误差小于0.01px、无水平溢出，桌面侧栏实际宽184px；已检查1440px截图。纯CSS小改经独立自查，未改脚本、数据或接口。最新远端CI见PR Checks，未合并或部署。

## 2026-09-21 首页信息轮播与可收起导航

- 移除榜单页大宣传标题与装饰图，保留紧凑榜单名和统计日。将计划更新时间、统计日期、收录数量放入轻量纵向文字轮播：8秒停留、650ms缓动，上一条/下一条/暂停可手动控制；悬停和键盘聚焦暂停，后台停止计时，减少动态效果与无JS显示完整静态文本。
- 数据新鲜度始终显示在轮播外，归档停更提示保留，计划更新时间不承诺发布完成；原倒计时与新数据刷新逻辑复用，轮播内秒级更新关闭aria-live播报。采用原生CSS/JS自实现，不增加动画库。
- 侧栏精简为项目榜单、分类目录、我的收藏、项目对比；时间范围统一到榜单内，累计榜也可返回各周期。7/30日历史或积累状态保留。Logo由88px改为40px横排，新增桌面侧栏隐藏/展开与本机偏好记忆；移动菜单独立、跨断点同步焦点和inert。浏览器返回/页面缓存恢复时重新读取侧栏偏好，专项返回导航回归通过。
- 独立审查指出归档事件榜误选中当前净增周期，已通过active=null修复并补回归；最终复核无待处理发现。
- 验证：站点31项、Astro81文件零诊断；新增7个浏览器场景在真实数据本地验证通过，包含320/390/1024/1440、跨路由与跨断点、计时推进/暂停、减少动态效果及无JS。手机与桌面Axe零违规，已检查展开/收起和手机截图。真实5947页/406张分享图构建及产物校验通过；打包后6项通过，返回场景因缓存页面无load事件超时，改为导航提交后验证实际状态并复跑通过。58场景及远端CI按本提交PR Checks跟踪；尚未合并或部署。

## 2026-09-21 详情页信息密度优化

- 将详情页改为紧凑项目概览：缩小项目名与区块标题，简介和操作集中在顶部，关键数据使用桌面五列/手机两列；项目速览与分类并排，删除重复简介，保留分类来源与缺失说明。
- 历史趋势默认可见，完整每日记录通过原生 details 展开，保留键盘和无 JavaScript 使用；收藏、对比、内容切换及详情数据属性契约保持兼容。面包屑的“发现”不再被长名称挤成竖排。
- 验证：站点31项单测通过，Astro77文件零诊断。两份用户截图对应真实项目在320/390/768/1440宽度与双语模式下无横向溢出、操作可触达、历史可键盘展开、无脚本错误；无JS完整30日记录可读。已检查桌面/手机截图。独立只读复核无待处理发现。
- 新增4个详情浏览器回归场景，覆盖长名称、首屏密度、移动按钮边界、键盘及无JS展开；旧历史行数断言改为先展开。远端完整构建与浏览器结果在本提交PR Checks跟踪。本步仅源码同步和本地预览，未合并或部署。

## 2026-09-21 新版补全测试夹具修正

- 3d116d7 的远端构建、数据及Worker检查通过，但浏览器回归为41通过/6失败。根因是夹具声明1.1却仍按旧榜单元数据生成缓存，正确的失效处理移除了首行中文和分类，影响筛选、中文、收藏及订阅断言。
- 夹具改为按新版同时间目录优先规则生成已有缓存，保留未分类项目与恶意文本用例；筛选回归增加非空条件断言。独立核对2500条翻译及2500条分类source_hash均匹配，最终复核无待处理发现。
- 此修正只改变测试数据及断言，生产缓存失效规则保持真实来源校验。全量浏览器和远端最终结果以本提交的PR Checks为准，不把此前失败的运行记作通过。


## 2026-09-21 补全来源显式迁移

- 翻译与分类 1.0 保持原榜单范围；1.1 显式增加当前发现目录，缺独立目录时回退观察目录。普通历史数据校验及部署不隐式升级；后续补全沿用已保存版本。等待项目用真实日期参与元数据选择，未知日期不会覆盖已知较新榜单。
- 新增隔离副本迁移工具：输入/输出均校验、核对输入摘要、拒绝覆盖/嵌套输出和未恢复 journal；迁移离线运行，保留仍有效缓存、新增及失效项目明确待处理。内容工作流新增手动 migrate_scope 模式，验证后提交数据但不部署 Pages；部署继续走固定 data_ref。
- 真实固定数据 `64bb0ad1a68f88da0a258702d90600ec1881d443` 在副本迁移后，补全来源由3970扩大为4898项，新增928项；待译1122、待分类1139。上述是榜单历史与当前目录的去重并集，不能当作当前目录数量。当前2000项中待分类由1078变为1087，原因是9项旧元数据缓存失效，并非已完成在线补全。
- 已修复独立审查发现的静态产物校验只接受1.0的阻断；新旧Schema、类型及分类双文件版本一致性纳入门禁，CI同时构建旧夹具和新1.1夹具并校验。
- 验证：Python139项通过，未知日期与迁移6项专项复跑通过，独立最终复核无待处理发现。真实迁移数据Astro76文件零诊断、5947页/406张分享图构建及产物校验通过，真实迁移数据4个浏览器场景通过。未调用在线模型，未迁移生产数据或部署。


## 2026-09-21 连续观察与发现目录分离

- 每日观察仍最多 2,000 项；独立发现目录最多 5,000 项，新发现先排队，不伪装成已取得每日样本。目录页面公开观察数量、排队数量、1/7/30 日实际可比数量及每项数据日期。
- 连续观察成员保护 30 日，满池时新固定项目也排队；保护结束后每日最多接纳容量的 5%（默认100项），首次建池例外。失效/转私有/归档项目移除，未知状态不补造；元数据刷新失败继续沿用原来的整批失败保护。
- state 1.3 与新 directory 1.0 保存观察记录、目录及容量；全部进入原发布 journal。已修复独立审查发现的第33天滚动历史导致起点漂移问题，加入34天连续运行与同日字节一致性回归。旧数据和旧 journal 保持原样复用，新采样才启用拆池。
- 固定数据提交 `64bb0ad1a68f88da0a258702d90600ec1881d443` 的离线报告：最近批次新增/移除各972项、可比1028项；新策略规划保护当前2000项，默认每天最多接纳100项。没有调用 GitHub 或生成未来样本，不能据此声称真实7/30日恢复。
- 验证：Python135项、站点31项、Astro76文件零诊断、固定旧数据直接校验通过。目录专项4个真实浏览器场景及产物校验通过（夹具含仅发现项目，2,001项目录/2,000项观察）；已检查移动端截图。独立最终复核无待处理发现。
- 发布限制：本步同步源码和运行手册；未修改线上数据、未合并或部署。上线后的请求预算、持续采样与真实覆盖仍需按运行手册验收。


## 2026-09-21 产品改进：补全优先级与旧数据兼容

- 中文及分类共享的处理队列优先最新日榜前100，再处理当前候选中的既有补全对象，之后才处理历史积压；已有有效缓存及人工覆盖保持复用，单批模型预算不变。
- 保留原来源范围及metadata/coverage/hash契约。只存在于当前目录、尚未进入既有补全来源的项目，本步仍不自动补全；该范围扩展需要显式数据版本迁移。
- 验证：优先级、翻译、分类24项专项回归与最终Python129项通过；未经协调的固定真实生产数据直接通过新版validator。独立审查指出的数据版本兼容风险已通过收窄本步范围解决，并复核通过。
- 未执行在线模型请求或修改生产数据，实际补全率变化须由后续真实任务验证。

本轮四个里程碑通过独立提交同步到 [PR #86](https://github.com/728792899-create/open-source-star-rank/pull/86)。前两步远端CI已通过；最终提交的远端完整CI以PR Checks为准。尚未合并主分支或部署Pages/Worker。


## 2026-09-21 产品改进：当前目录与可核对的选型信息

- 分类入口 `/board/` 改为当前候选目录，完整包含本批 2,000 个项目；`/discover/` 每页100项静态分页，含待分类兜底，旧事件分类总览保留在 `/board/archive/`。
- 7/30日导航和页面区分当前、历史与积累状态。对比页按稳定ID核对最多4个本站详情，显示来源日期、创建及最近提交日期，失败保留并标旧值（R03）。
- 相似项目优先匹配共同场景，显示分类依据及数据日期，不推断功能等价或商业许可。
- 移除种子后解除固定状态，改名保留ID及首次观察历史（R11）；Top500迁移只比较前自然日（R12）。
- 修复本地静态预览缺少SVG/WebP类型导致logo破图；浏览器以naturalWidth验证图标实际加载。保留现有品牌与配色，调整目录/对比标题和控件。
- 验证：Python129项（含下一步补全优先级）、站点31项、Astro76文件零诊断；完整夹具47浏览器场景通过。最终真实数据构建5,947页/406张分享图、产物检查通过；真实数据4个浏览器场景再次通过，核对2,000项跨页无遗漏或重复、无JS移动端、对比失败恢复及移除期间请求返回，已检查实际截图。
- 独立只读复核无剩余阻断项。源码/远端CI/生产部署分别跟踪；本步未修改线上数据或部署Worker。
- 完整范围及后续验收见 [产品改进清单](PRODUCT_IMPROVEMENTS_2026-09-21.md)。当前目录仍有待翻译/待分类项目，不能把入口可用称为内容全部补齐。


## 2026-09-21 产品改进：收藏与授权异常

- R05：GitHub 状态未知时明确标注“点击重查”，不借用本机收藏、不进行 PUT/DELETE；错误后的重试先读取真实状态。
- R06：批量同步遇到 401、权限不足、限流、上游或网络异常及时停止，保留成功项与未确认项；401 清理本次失效会话，旧请求不能覆盖新账号。
- R08：本次单个收藏意图绑定 OAuth 浏览器证明和成功会话；回原项目后确认或取消，只处理该项目，不自动同步历史收藏。
- R13：批量成功项目通知详情按钮刷新；初始化锁与版本保护防止并发查询覆盖操作结果。
- 验证：站点 28 项、Worker 20 项、Worker 类型/preflight、Astro 66 文件零诊断；浏览器 43 场景通过（42 项首轮通过，1 项无障碍扫描因并行超时单独复跑通过）。独立复核发现的两个并发缺口已修复并复核通过。
- 部署边界：以上均为源码及模拟账号验证，未操作用户真实 Star。Worker 仍需恢复部署凭据并完成真实 OAuth/D1 验收，不能以 Pages 发布代替。


## 2026-09-21 产品改进：发布完整性与分享图

- 累计 Top 1000 在每个搜索查询内核对唯一仓库 ID 和预期页长；异常从第一页完整重试一次，仍异常则保留上一版，不再以低位仓库掩盖遗漏（R01）。
- 分享图仅使用明确的 CJK 字体，构建前用实际渲染器核对中文字形；五份建站 workflow 安装 Noto CJK。缺字体或缺字时在清空输出前失败，避免发布方框文字。
- 验证：Python 123 项、站点 22 项通过；独立只读复核无阻断发现。已检查含中文项目名、品牌及标点的实际 PNG。Ubuntu 字体安装与全量产物仍由本次 PR 的远端 CI 验证；不承诺不同字体版本跨系统逐字节一致。
- 本步为源码同步；生产 Pages 和 Worker 发布状态另行记录。后续继续修复收藏、同步及产品入口。


## 2026-09-21 king AI 纯字标

- **最终设计**：按用户最新要求取消皇冠和独立 K，小写 `king` 在上、大写 `AI` 在下，均为相同字号和字重；橙色字形配圆角暖白底板。保留「开源星榜」作为产品说明。
- **接入**：正式站侧栏与手机展开导航、SVG/PNG favicon、Apple 主屏幕图标、默认和榜单分享卡片统一使用新标识；字体转轮廓，部署不依赖本机字体。原始参考、提示词、可下载 SVG/PNG 与导出命令见 [品牌文件](design/kingai-logo/README.md)。
- **验证**：站点 19 项单测通过；Astro 64 文件零诊断；真实数据的桌面 1487px、平板 1024px、手机 390px 浏览器检查通过，标识与图标路径返回 200、无溢出或页面脚本错误。截图见 [桌面](evidence/kingai-logo-20260921/desktop.png)和 [手机导航](evidence/kingai-logo-20260921/mobile-navigation.png)。真实数据完整构建 5,750 页、406 张榜单分享卡片与产物校验通过；四份 PNG 重导出逐字一致，独立代码复核无待处理发现。远端 CI、合并发布结果在本次 PR 跟踪。
- **范围**：仅更新品牌展示与资产，不改变排名、采集、登录和数据覆盖规则；正式发布后还需验证页面标识、图标及分享卡片。

## 2026-09-21 正式 UI 与真实数据迁移

- **实现**：将已选定的侧栏布局、暖白橙色主题、顶部搜索及项目操作迁入生产 Astro；继续消费真实候选池、日榜 Top 500（五页）与累计 Top 1000。原型仍保留六条演示记录，正式站不依赖演示数组。
- **交互与修复**：保留 URL 筛选、分页、中文/原文切换、详情、收藏、最近访问和四项目对比。R02 使用有符号趋势、零基线和缺失断点；R04 网络失败清理缓存并提供原地重试。手机菜单支持键盘焦点恢复，无脚本状态保留跨页导航。
- **本地验证**：站点 19 项单测、Astro 63 文件零诊断、完整 5,079 页构建与产物校验、37 项浏览器回归全部通过。真实数据桌面/手机/平板和历史榜截图及比较过程见 [设计验收](../design-qa.md)。测试夹具和真实数据预览隔离，未把夹具上传为正式数据。
- **独立复核**：修复历史榜列宽/占比图、平板网格、手机操作裁切和无脚本导航；最终代码审查无阻断发现。浏览器补齐日期文字对比度、侧栏地标名称与滚动遮挡修复后回归全绿。
- **补充子页面验收**：发现并修复手机对比/详情等无榜单输入框页面的搜索入口隐藏问题；真实浏览器验证了菜单贴合页头、Escape 恢复和返回榜单搜索，已加入同一回归用例。
- **发布门禁**：本步从 main `907e00e` 开发。源码同步后仍须完成远端 CI，使用主分支代码及精确数据提交运行 `deploy_existing`，再对比生产页面和数据索引；最终远端检查、合并与部署链接记录在 [PR #84](https://github.com/728792899-create/open-source-star-rank/pull/84)。源码检查通过不代替生产验收。
- **边界**：没有扩大 GitHub 收录范围；7 日/30 日榜仍要求有效连续快照。Worker/D1 部署与真实 OAuth 授权恢复不包含在本次 UI 发布中，其他已知缺陷仍列于 [问题清单](KNOWN_ISSUES.md)。

## 2026-09-21 每日数据恢复与调度加固

- **实际恢复**：[正式采集与发布任务](https://github.com/728792899-create/open-source-star-rank/actions/runs/35529436407) 已成功。北京时间 02:34 捕获有效快照，与前一天 02:38 的快照形成 1,435 分钟有效窗口；线上最新日榜从 2026-09-13 恢复为 **2026-09-20，500 项**。随后延迟到达的 [定时任务](https://github.com/728792899-create/open-source-star-rank/actions/runs/35530407054) 成功复用同日快照，未改变采样时间。
- **根因证据**：历史多次定时任务延迟至北京时间 03:00 后，生产采样门禁正确拒绝；仅有最新快照时，旧监控没有检查昨日榜日期，会误认为榜单恢复。另有在线模型补全串行阻塞日采任务的风险。
- **本批次修复**：保留 00:20 目标，增加 00:07、00:47 触发；同日幂等复用仍保留 03:00 门禁。主任务只离线协调中文/分类缓存（包括 validate 模式），在线补全继续由独立工作流处理。复用快照和恢复 journal 均检查有效性。
- **发布与监控**：watchdog 在 01:15、03:15 检查今日有效快照及昨日榜。关闭故障前还须将线上索引与本次精确数据提交逐字核对。页面新鲜度使用实际展示榜单的 window_end，避免新采样掩盖旧榜单。
- **本地验证**：Python 119 项、站点 18 项、Astro 61 文件零诊断、两份 workflow YAML 解析通过；独立只读审查发现并修复了发布版本比对缺口，最终无阻断发现。本地完整夹具构建及浏览器 31 项重跑通过（首次卡片渲染超时）；[新版监控真实运行](https://github.com/728792899-create/open-source-star-rank/actions/runs/35551652796) 也已通过。最终远端 CI/合并部署状态见 [PR #83](https://github.com/728792899-create/open-source-star-rank/pull/83)。
- **边界**：已验证一次真实恢复及同日定时复用，不能据此保证下一天准点调度。GitHub 调度延迟仍属外部限制；7 日、30 日榜需继续累积有效窗口，缺失日期不补造。Cloudflare 授权恢复和六项目 UI 原型接入真实数据仍是后续工作。

上一批次 PR #76 已合并 main（c29a231），Pages 发布成功，依赖告警清零；Worker 授权阻塞状态未改变。

## 2026-09-20 修复与上线批次

已核实 PR #70 合并至 main（ae90c75）；以下为后续独立修复步骤，不改变六项目 UI 原型的产品范围。

1. **退出登录撤销修复（R07）**：允许的 Origin 下按本站 bearer 摘要幂等删除会话，不调用 GitHub 刷新或解密；数据库失败仍返回失败。浏览器立即退出本机状态，服务端撤销未确认时在可见状态区提示，旧退出请求不能影响新登录。
   - 验证：Worker 14 项测试、TypeScript、preflight；站点 16 项测试；Astro 检查。覆盖临期/过期/密文异常、重复退出、会话隔离、错误 Origin、数据库故障及前端延迟响应。
   - 独立审查指出提示原先位于折叠收藏面板内，已改为页面可见的 aria-live 状态区并增加 DOM 状态断言。
   - 当前生产 Worker 凭据已过期、GitHub 未配置部署 secret；源码同步和 Pages/Worker 上线分别跟踪，不能由本地通过推断部署完成。

2. **依赖与凭据文件加固**：核对 GitHub 的 9 条原型依赖告警，Vite 升至 6.4.3，并更新锁文件中的 Browserslist/PostCSS/nanoid/baseline-browser-mapping 等依赖；加入原型 CI 审计门禁及原型/Python Dependabot，根目录忽略 .env。
   - 在独立临时副本执行 npm ci、构建、15 项原型测试和 moderate 级 npm audit，审计为 0；没有修改现有预览共享的 node_modules。
   - 默认分支告警关闭与生产部署状态需在合并后另行核实。

3. **数据缓存与构建边界修复（R09、R10）**：撤销人工覆盖后失效旧缓存；失败统计按当前 pending 身份集合核对，旧缓存首次在线处理迁移为实际观测 ID。社交卡片仅消费通过 Schema 校验的清单，渲染 Worker 再校验输出文件名；本地预览隔离畸形 URL、越界路径、缺失文件与流错误。模型异常返回使用有限重试与既有回退。
   - 验证：Python 114 项、站点 18 项、完整夹具 5,079 页构建与产物检查、浏览器 30 项全部通过；独立只读复核无剩余阻断项。最终提交仍需经过远端完整 CI（含可复现构建和 Lighthouse）后合并。
   - 升级顺序、旧 Schema 回滚限制及范围见 [本轮修复记录](SECURITY_REMEDIATION_2026-09-20.md)。此前安全审计的未完成范围保持不变，不能把本轮回归当作全量审计完成。

以下为 2026-09-19 同步时的历史记录，实时发布结果以本节与 PR #76 关联部署为准。

更新日期：2026-09-19。开发分支：[`codex/project-upgrade-20260919`](https://github.com/728792899-create/open-source-star-rank/tree/codex/project-upgrade-20260919)。

## 当前交付范围

此次将此前本地完成的修复、UI 与交互优化按里程碑补入公开仓库，提交时间反映本次同步时间。生产 Astro 站点与 React 原型各自保留入口；本轮只同步源码及证据，未合并主分支、部署 Pages/Worker、迁移远程 D1 或执行真实账号操作。

| 步骤 | 状态与成果 | 证据与限制 |
| --- | --- | --- |
| 1. 业务审查修复 | 已完成并推送 [`6ae84b1`](https://github.com/728792899-create/open-source-star-rank/commit/6ae84b1)：采集/历史/契约、认证绑定与会话竞态、注入/分页、依赖和质量校验。 | [第一轮修复记录](AUDIT_FIXES_2026-09-19.md)。本次重新通过 Python 110、前端逻辑 13、Worker 10、类型/preflight、Astro 59 文件零诊断及初始化构建/产物校验。 |
| 2. UI 设计与交互优化 | 已完成并推送 [`995d921`](https://github.com/728792899-create/open-source-star-rank/commit/995d921)，已纳入 `prototypes/starrank-ui/`，包含选定视觉、源码、素材来源、响应式截图和验收记录。 | [原型入口](../prototypes/starrank-ui/README.md)与 [UI 验收](../prototypes/starrank-ui/design-qa.md)。新路径构建和 15 项测试通过；六条演示数据，未接真实 API/OAuth。 |
| 3. 持续同步与质量门禁 | 增加根目录协作规范、独立 UI CI、公开进度和剩余问题清单，随当前 PR 审查。 | 每个完成并验证的步骤提交、推送并记录；CI 仅校验，不附带生产部署。远端最新结果以 PR Checks 为准。 |
| 4. 第二轮业务缺陷闭合 | 剩余待实施：9 项 P2、1 项 P3；R07、R09、R10 已修复。 | [R01–R13 清单](KNOWN_ISSUES.md)。这些问题在第一轮修复后确认，不能以正常测试通过代替异常场景修复。 |
| 5. 正式 UI 与真实数据 | 待实施：迁移到业务页面，接入真实仓库列表、榜单、分类与数据状态。 | 当前业务最多 2,000 候选、净增 Top 500、累计 Top 1000；原型只有六项。 |
| 6. 扩大收录和上线验收 | 待设计与实施：分层发现、增量采集、索引分页、更新监控；随后执行合并和部署任务。 | 明确实际覆盖范围、刷新周期与失败恢复，不将现有候选池称为 GitHub 全量镜像。 |

## 验证记录的口径

- 上表列明本次源码同步时重新运行的检查。第一次运行 Astro 检查时，shell 未使用已安装依赖的 Python 环境，缺少 jsonschema；指定项目虚拟环境后检查与构建通过。
- 第一轮原始验收另有完整夹具 5,079 页、可复现构建、29 项浏览器检查及 Lighthouse 结果，详见原修复记录；这些属于之前的本地证据，不冒充本次全部重跑。
- UI 浏览器截图来自本轮原型验收；纳入仓库保持源码与资源内容，重新执行构建和自动测试。没有声称完成真实手机、屏幕阅读器或线上登录验收。
- 远端 CI、生产状态、已知问题分别记录，不因源码上传而把后续工作标为完成。

## 后续每步如何更新

完成一个明确目标后运行相应检查，提交本步代码与进度，推送当前开发分支，核对远端提交并查看 CI；PR 说明随最终范围更新。未完成实验、依赖缓存、凭据和生成测试数据不进入提交。具体规则见 [AGENTS.md](../AGENTS.md)。
