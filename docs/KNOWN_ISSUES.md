# 待修复问题与正式产品接入

更新日期：2026-09-19。第一轮已完成的修复见 [审查修复记录](AUDIT_FIXES_2026-09-19.md)。以下是第二轮在修复后源码上通过诊断确认的剩余问题，**尚未修复**；此次上传仅同步既有成果，不将诊断通过当作修复通过。

## 已确认业务缺陷

| 编号 / 优先级 | 触发条件与影响 | 后续修复及验收 |
| --- | --- | --- |
| R01 / P2 | 历史 Top 1000 分页漂移，跨页重复被去重后，较低名次补位，仍标记完整。 | 在 `tools/alltime_star_rank.py` 检查单次查询跨页重复与数量异常；段级重试或保留上一版。覆盖 total_count 不变但漏第 101 名的情况。 |
| R02 / P2 | Astro 榜单微型趋势对数值取绝对值，正负序列图形相同。 | 统一 `RankingView.astro` 与 `ranking-explorer.mjs` 的有符号绘图、零基线与缺失处理。验证增长、下降、跨零、缺失。UI 原型的修正尚未迁移到业务站。 |
| R03 / P2 | 本机对比保存旧项目元数据，跨版本访问后仍称为“当前”。 | 按项目 ID 刷新元数据，或明确标注快照日期并提供更新；测试两版数据之间的访问。 |
| R04 / P2 | 深度筛选首次请求失败后，拒绝的 Promise 留在缓存，网络恢复仍无法重试。 | 清理失败缓存并加入重试入口，避免旧请求清除新请求；无需刷新整页即可恢复搜索。 |
| R05 / P2 | GitHub Star 查询失败时，用本机收藏状态代替远端未知状态，按钮文案与 PUT 行为相反。 | 明确显示状态未知并提供重查，未知时不切换远端 Star；覆盖 403、限流、网络失败及本机/远端不一致。 |
| R06 / P2 | 批量同步吞掉上游 401，最终返回 200，前端仍显示登录。 | 认证失效时停止批次、返回部分结果，区分权限与限流；保留旧请求不可清理新会话的并发保护。 |
| R07 / P2 | 退出先刷新临近到期的 GitHub Token，上游故障时 D1 会话未撤销。 | 用站点 bearer token 摘要幂等撤销本地会话，退出不依赖 GitHub 刷新；默认配置影响最后 60 秒附近，不能夸大为完整八小时。 |
| R08 / P2 | 未登录时选“登录并同步”，仅跳转授权，遗失刚选择项目的收藏意图。 | 将单个明确动作与本次登录绑定，成功后完成或确认；覆盖成功、取消、失败，不自动同步其他历史收藏。 |
| R09 / P2 | 人工补齐翻译/分类后，离线流程继承旧失败数，可能得到 pending=0、failed=1 并阻断发布。 | 失败集合与当前待处理集合求交，覆盖人工补齐、源项目减少及离线构建。 |
| R10 / P2 | 删除人工翻译/分类覆盖条目后，缓存仍固化 manual 结果。 | 当前配置撤销人工来源时让缓存失效；在线重新生成、离线标待处理，两个管线均覆盖。 |
| R11 / P2 | 删除固定种子后，历史 pinned 状态仍继承，挤占新候选。 | 由当前种子配置按稳定 ID 重算固定集合；覆盖增删、改名、容量淘汰。 |
| R12 / P2 | Top500 迁移用上一个存在日期计算排名变化，跨过缺失自然日。 | 仅比较前一自然日，缺失则留空；覆盖日榜、语言和周期榜。已在当前格式且跳过迁移的数据不走此路径。 |
| R13 / P3 | 项目页内批量同步成功后，当前项目按钮仍保留旧 GitHub Star 状态。 | 广播成功项目 ID 或重新查询，覆盖部分成功及随后直接操作。 |

本地复现使用临时数据、模拟请求或内存 SQLite，没有进行真实账号 Star 操作。本次同步复核再次确认 R05–R08、R13；其余沿用同日第二轮审查证据。优先处理 R01、R02、R05–R07，再闭合其余异常和维护流程。

## 产品接入路线

1. **迁移已验收 UI 并接入真实数据。** 将 `prototypes/starrank-ui` 组件迁移到 Astro 业务页面，接入已有榜单、仓库详情、语言与分类数据，保留加载/空数据/失败/陈旧状态；验收真实项目数量、筛选、分页、详情、对比及数据日期。
2. **扩展收录与索引。** 当前业务配置为最多 2,000 个持续候选、净增 Top 500、累计 Top 1000；六条数据只属于 UI 原型。分层发现、分段采集、增量刷新、去重、搜索索引和分类需要单独实现，不能把现有榜单称为 GitHub 全量镜像。
3. **说明覆盖与时效。** 展示已收录数量、最近成功时间、各管线健康和完整性；搜索、采集或速率限制失败时保留上一有效快照。以可验证覆盖范围为准，不承诺全站实时无遗漏。
4. **提升选型价值。** 将原型中的用途、部署、平台、资源、许可名称和维护状态接入有来源的信息；未知字段留空，相似项目展示推荐理由。
5. **完善交付与运维。** 区分采集恢复、连续基线重建和榜单更新，记录源码/数据版本与构建产物，演练回滚；以真实数据量度量增量构建成本。

这些是后续计划，不是已经上线的功能。当前源码同步、主分支合并、Pages 发布和 Worker/D1 升级是不同交付阶段，状态见 [项目进度](DEVELOPMENT_PROGRESS.md)。
