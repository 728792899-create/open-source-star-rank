# 待修复问题与正式产品接入

更新日期：2026-09-21。第一轮已完成的修复见 [审查修复记录](AUDIT_FIXES_2026-09-19.md)。以下是第二轮诊断的问题清单；R01–R13 均已完成源码修复；授权相关生产部署及真实账号验收仍待完成。部署与验证状态见 [本轮修复记录](SECURITY_REMEDIATION_2026-09-20.md)，不将源码修复等同生产验收。

## 无人值守启用边界（2026-09-23）

采样先保存、离线重放、受限重试、补全退避队列、Cloudflare独立监测和R2恢复校验已实现于开发分支。2026-09-23 Cloudflare本机授权已恢复；用户无法订阅R2，改用Worker + 专用D1调度，R2独立备份默认停用。PR #86 已合并，GitHub App 已完成仅限本仓库的授权，Worker + D1 已部署并启用自动恢复；23:45 真实 Cron 确认授权、自动恢复与状态持久化，Pages 发布运行 35883388958 成功。2026-09-24 已完成真实自动重试、1,993 项完整采样、原始记录校验和 Pages 发布闭环，运行 35890977791 成功；延迟调度 35910702213 成功复用当天快照。当前处于断档后的新基线，连续日榜和 31 日长期运行仍待后续真实样本；详细证据见最新进度记录。启用步骤见[运行器说明](../ops-worker/README.md)。

长期存储仍有资源上限；原始记录/历史对象不会自动删减。独立运行器无法消除GitHub全平台故障造成的真实采样缺口，31日连续采样与实际云灾难恢复尚待验收。

## 模型补全服务迁移（2026-09-24）

已核实 GitHub Models 于 2026-07-30 退役，旧接口现在返回 HTTP 200 / text/plain / `OK`，因此此前 JSON 解析失败不是可通过继续重试恢复的模型输出问题。已移除默认旧服务调用及 Actions models 权限，补全改用显式配置的兼容接口和专用密钥，配置不完整只离线验证。用户已改用 DeepSeek 官方 API；`deepseek-flash` 已通过两个真实项目的翻译和分类小批量验证，专用密钥已写入 GitHub Secret。PR #93 合并后的真实运行 35960199510 已成功发布：中文 3958/3966，分类 3966/3966；尚有 8 条中文失败。当前步骤修复长标题、普通英文误判和长描述摘要规则，临时目录真实小批量 8/8 通过，尚待合并后生产运行确认。逐项错误反馈、每次运行的请求/Token/失败记录和可追溯估算费用已上线。费用不是账单，也尚无硬性美元预算熔断；未知用量、未知模型或价格过期时不估算费用。

## 已确认业务缺陷

| 编号 / 优先级 | 触发条件与影响 | 后续修复及验收 |
| --- | --- | --- |
| R01 / 已修复 | 历史 Top 1000 分页漂移，跨页重复被去重后，较低名次补位，仍标记完整。 | 2026-09-21：单次查询检查跨页重复与页长异常，整段重试一次后拒绝发布，保留旧文件。覆盖 total_count 不变但漏第 101 名、截断页和恢复重试。 |
| R02 / 已修复 | 旧版趋势对数值取绝对值，正负图形相同。 | 2026-09-21 正式站使用统一有符号 canvas 绘图，包含零基线和缺失断点；增长、下降、跨零、无效值及筛选后恢复均有回归。 |
| R03 / 已修复 | 本机对比保存旧项目元数据，跨版本访问后仍称为“当前”。 | 2026-09-21：按稳定 ID 获取本站详情并刷新对比，标明来源日期；失败保留旧值并可重试，异步结果不会恢复已移除项目。真实数据与夹具浏览器通过。 |
| R04 / 已修复 | 旧版深度筛选失败会保留拒绝的 Promise。 | 2026-09-21 失败后清理共享请求缓存并提供“重试深度筛选”，浏览器注入一次 503 后可原地恢复，不刷新页面。 |
| R05 / 已修复待部署 | GitHub Star 查询失败时，用本机收藏状态代替远端未知状态，按钮文案与 PUT 行为相反。 | 明确显示状态未知并提供重查，未知时不切换远端 Star；覆盖 403、限流、网络失败及本机/远端不一致。 |
| R06 / 已修复待部署 | 批量同步吞掉上游 401，最终返回 200，前端仍显示登录。 | 认证失效时停止批次、返回部分结果，区分权限与限流；保留旧请求不可清理新会话的并发保护。 |
| R07 / 已修复待部署 | 退出先刷新临近到期的 GitHub Token，上游故障时 D1 会话未撤销。 | 2026-09-20 已改为按 bearer 摘要幂等撤销，保留 Origin 门禁；前端可见提示撤销未确认。Worker/前端回归通过，生产状态见进度记录。 |
| R08 / 已修复待部署 | 未登录时选“登录并同步”，仅跳转授权，遗失刚选择项目的收藏意图。 | 将单个明确动作与本次登录绑定，成功后完成或确认；覆盖成功、取消、失败，不自动同步其他历史收藏。 |
| R09 / 已修复 | 人工补齐翻译/分类后，离线流程继承旧失败数，可能得到 pending=0、failed=1 并阻断发布。 | 2026-09-20：按实际失败 ID 与当前 pending 求交；兼容旧缓存并避免重复重试累加失败数。人工补齐、源项目减少、离线与在线迁移回归通过。 |
| R10 / 已修复 | 删除人工翻译/分类覆盖条目后，缓存仍固化 manual 结果。 | 2026-09-20：撤销人工来源即失效；在线重新生成、离线标待处理，翻译与分类均有回归覆盖。 |
| R11 / 已修复 | 删除固定种子后，历史 pinned 状态仍继承，挤占新候选。 | 2026-09-21：按当前配置重算固定状态，别名解析后保留稳定 ID 和历史。移除、改名及容量淘汰回归通过。 |
| R12 / 已修复 | Top500 迁移用上一个存在日期计算排名变化，跨过缺失自然日。 | 2026-09-21：仅比较前一自然日，缺失留空；日榜、语言和7/30日榜回归通过。已是当前格式且跳过迁移的数据保持不变。 |
| R13 / 已修复待部署 | 项目页内批量同步成功后，当前项目按钮仍保留旧 GitHub Star 状态。 | 广播成功项目 ID 或重新查询，覆盖部分成功及随后直接操作。 |

本地复现使用临时数据、模拟请求或内存 SQLite，没有进行真实账号 Star 操作。本次同步复核再次确认 R05–R08、R13；其余沿用同日第二轮审查证据。2026-09-21 已修复 R01、R05、R06、R08、R13，并补充异常与浏览器回归；R03、R11、R12 也已闭合；后续工作转入下述产品与部署验收。

## 产品接入路线

1. **正式 UI 迁移已实施。** Astro 业务页面采用原型视觉，接入已有真实榜单、详情、语言和分类数据，保留加载/空数据/失败/陈旧状态。视觉与交互证据见 [UI 验收](../design-qa.md)，GitHub/部署结果见进度记录。
2. **扩展收录与索引。** 新源码支持最多 2,000 个受保护的每日观察成员与独立 5,000 项发现目录、净增 Top 500、累计 Top 1000；新采集才启用拆池，旧数据仍可部署。补全 1.1 显式迁移已实现，生产迁移、实际在线补齐与连续7/30日仍待验收。六条数据只属于 UI 原型；当前能力不等于 GitHub 全量镜像。
3. **说明覆盖与时效。** 展示已收录数量、最近成功时间、各管线健康和完整性；搜索、采集或速率限制失败时保留上一有效快照。以可验证覆盖范围为准，不承诺全站实时无遗漏。
4. **提升选型价值。** 将原型中的用途、部署、平台、资源、许可名称和维护状态接入有来源的信息；未知字段留空，相似项目展示推荐理由。
5. **完善交付与运维。** 区分采集恢复、连续基线重建和榜单更新，记录源码/数据版本与构建产物，演练回滚；以真实数据量度量增量构建成本。

其中未完成项目仍是后续计划，已完成项的发布结果以进度记录为准。当前源码同步、主分支合并、Pages 发布和 Worker/D1 升级是不同交付阶段，状态见 [项目进度](DEVELOPMENT_PROGRESS.md)。
