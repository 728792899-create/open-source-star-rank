# 2026-09-21 正式站 UI 迁移验收

final result: passed

## 对照与状态

- 视觉目标：`prototypes/starrank-ui/evidence/visual-target.png`，1487 × 1058 px。
- 实现：`docs/evidence/ui-production-20260921/desktop.png`，1487 × 1058 CSS px，deviceScaleFactor 1；同尺寸、浅色主题、发现页默认榜单。
- 其他证据：同目录 `mobile.png` / `mobile-rows.png` / `mobile-menu.png`（390 × 844）、`tablet.png`（768 × 1024）、`alltime.png`（1487 × 1058）、`alltime-mobile.png`（390 × 844）。均为 Chrome 实际渲染截图。
- 源图与桌面实现放在同一比较输入中查看；在原始分辨率检查了顶部导航、标题、项目名称、标签、指标与操作区。文字与图标可直接辨认，无需二次裁剪放大。手机截图另检验抽屉展开和滚动列表。
- 原型六项演示、已有两个对比项；生产为真实 2026-09-20 日榜（500 项、每页 100 项）及空的本机收藏状态。数据和状态不同，不作项目名、图形形状或对比计数的逐像素比较。
- IAB 自动化初始化两次超时，使用项目 Playwright 和本机已安装 Chrome 完成渲染验收；没有把静态源码视为浏览器证据。

## 必检表面

| 表面 | 结论 |
| --- | --- |
| 字体与排版 | 沿用 Inter + 中文系统字体，标题、正文与小标签层级清楚；长仓库名截断，详情页提供全文。手机标题、登录和语言控件不再互相挤压。 |
| 间距与布局 | 保留左侧栏、顶部搜索、发现标题、时间范围和数据表。将采样详情折叠，宽屏合并操作与周期栏；正文列表更早进入首屏。生产保留四维筛选、历史日期、下载和分享，因此比六项目原型占用更多空间，这是明确的产品约束。 |
| 配色与状态 | 使用暖白纸色、深橙选中态及绿色增长；日期不可点击状态仍符合文本对比度。键盘焦点、hover、reduced-motion 有定义。 |
| 资产质量 | 复用原型原始 WebP 插画和同一官方 Tabler 星形图标；项目采用真实 GitHub 所有者头像。没有将六个演示品牌标识复用于其他仓库。来源及许可证见 `site/public/assets/ui/README.md`。 |
| 文案与内容 | 保留候选池覆盖说明、统计日、缺失日期与分类/翻译待补状态；不宣称 GitHub 全量镜像。未配置授权时显示“登录待配置”，不模拟登录成功。 |

## 发现与修复历史

1. **[P2，已修] 首屏说明过高、手机顶部按钮断行。** `desktop-before.png` / `mobile-before.png` 为初版证据。将统计与方法放入可展开区，修正旧移动样式的固定按钮宽度。后续 `desktop.png` / `mobile.png` 确认标题和控件清晰。
2. **[P2，已修] 历史榜被头像列和新趋势样式误伤。** 为历史榜保留独立五列和单列项目文字；只禁用新 canvas 趋势区域的旧伪元素。`alltime.png` 显示名称、语言、占比和星数，手机版文字区域正常。
3. **[P2，已修] 平板继承双列卡片、手机嵌套操作网格裁切按钮。** 显式重置旧 grid-area/table-body 并覆盖两种密度；`tablet.png`、`mobile-rows.png` 的项目、指标与操作均完整。手机筛选取消旧透明吸顶行为，滚动时不盖住第一行。
4. **[P2，已修] 日期不可点击文字对比不足。** 使用正文次级文字色替代浅灰。真实数据桌面与手机 axe WCAG A/AA 扫描均零违规。
5. **[P2，已修] 移动端无 JavaScript 时没有主导航。** 加入静态导航；菜单打开聚焦首个导航项，Escape 关闭并恢复按钮焦点；跳过导航链接层级高于侧栏。

6. **[P2，已修] 新侧栏与原有倒计时区域未区分无障碍地标名称。** 完整 axe 扫描发现后，为侧栏与对比托盘补充唯一可访问名称；该修正不改变可见布局。

## 交互验收与边界

已验证：搜索从 URL 恢复、筛选重排与分页、清除恢复 SSR、加载失败后原地重试、静态无脚本导航、两种密度、多尺寸操作边界、复制/分享、收藏/最近查看/四项目对比、原文切换、历史与详情路由。趋势使用同一有符号比例尺、零基线和缺失断点，SSR、筛选结果与清除后的 canvas 都会绘制。真实数据页面没有未处理脚本异常。

没有新增尚未接通的演示控件。真实 OAuth、Cloudflare Worker/D1 升级与 GitHub 账号操作未在本次验收中完成；它们保持单独交付边界。未执行实体手机或人工屏幕阅读器测试。

## 后续 P3 润色

可在不隐藏来源、分类和统计口径的前提下，继续研究长中文仓库名与多标签行的密度；本次保留现有紧凑/详细切换，不为追求六行示意图而截掉真实信息。
